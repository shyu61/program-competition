#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)

int main() {
    string s; cin >> s;
    int l; cin >> l;
    if (s.size() >= l) cout << "Yes" << endl;
    else cout << "No" << endl;
}
